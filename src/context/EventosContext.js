import React, { Component } from 'react';

const EventosContext = React.createContext();
export const { EventosConsumer } = EventosProvider.Consumer;

class EventosProvider extends Component {
    state = {  }

    eventos
    render() { 
        return (  );
    }
}
 
export default EventosProvider;